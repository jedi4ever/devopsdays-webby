---
extension: html
filter:    erb
---
<% 
@eventhome = @page.directory.split(File::SEPARATOR)[0..1].join(File::SEPARATOR)
@rootpage = @pages.find(:filename => "index", :directory => @eventhome)
@eventtitle = render(:partial => File.join(@eventhome, "_title"))
@pagetitle = "#{@eventtitle} - #{@page.title}"
if @page.proposal
  @pagetitle = "#{@eventtitle} -  Proposal"
end 
%>

<!DOCTYPE html>
<html lang="en">
    <head>
        <%= render(:partial => '/html_head') %>
    </head>

    <body>
        <div id="wrap">
        <%= render(:partial => '/header') %>
        <div class="container">
            <div class="row">
                <div class="col-sm-8">
                    <div class="row">
                        <div class="col-sm-12">
                            <div class="page-header">
                                <h1><%= @pagetitle %></h1>
                            </div>

                            <%= render(:partial => File.join(@eventhome,'_menu')) %>

                            <% if @page.proposal %>
                            <p>Back to <a href='..'>proposals overview</a> - <a href='../../program'>program</a></p>
                            <% end %>

                            <%= @content %>
                        </div>
                        <div class="col-sm-12">
                            <% if !@page.blog_post.nil? || !@page.proposal.nil? %>
                                <%= render(:partial => '/disqus_comments') %>
                            <% end %>
                        </div>
                    </div>
                </div>

                <% if @page.sidebar != false %>
                <div class="col-sm-4">
                    <div class="align-sidebar">
                        <% if @rootpage.platinum == true %>
                            <h1>Platinum sponsors</h1>
                        <% elsif @rootpage.gold.nil? || @rootpage.gold == true %>
                            <h1>Gold sponsors</h1>
                        <% end %>

                        <%= render(:partial => File.join(@eventhome, "_sponsors")) %>
                    </div>
                </div>
                <% end %>
            </div>
        </div>
        </div>

        <script src="https://code.jquery.com/jquery-1.10.2.min.js"></script>
        <script src="/js/bootstrap.min.js"></script>

        <%= render(:partial => '/googleanalytics') %>
        <%= render(:partial => '/footer') %>
    </body>
</html>
