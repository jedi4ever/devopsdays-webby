---
extension: html
filter:    erb
---
<!DOCTYPE html>
<html lang="en">
    <head>
        <%= render(:partial => '/html_head') %>
    </head>

    <body>
        <div id="wrap">
        <%= render(:partial => '/header') %>
        <div class="container">
            <div class="row">
                <div class="col-md-<%= @page.sidebar != false ? '8' : '12' -%>">
                    <div class="row">
                        <div class="col-sm-12">
                            <div class="page-header">
                                <h1><%= @page.title %></h1>
                            </div>
                            <%= @content %>
                        </div>
                        <div class="col-sm-12">
                            <% if !@page.blog_post.nil? || !@page.proposal.nil? %>
                                <%= render(:partial => '/disqus_comments') %>
                            <% end %>
                        </div>
                    </div>
                </div>
                <% if @page.sidebar != false %>
                <div class="col-md-4">
                    <%= render(:partial => '/twitterfeed') %>
                    <%= render(:partial => '/past_events') %>
                </div>
                <% end %>
            </div>
        </div>
        </div>

        <script src="https://code.jquery.com/jquery-1.10.2.min.js"></script>
        <script src="/js/bootstrap.min.js"></script>

        <%= render(:partial => '/googleanalytics') %>
        <%= render(:partial => '/footer') %>
    </body>
</html>
